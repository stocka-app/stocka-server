# Falla con error claro en peer dep conflicts en lugar de override silencioso.
# Cuando actualices una dep mayor (ej. TypeScript 5→6), si npm install falla
# con peer dep conflict: actualiza también los paquetes relacionados (ej. typescript-eslint).
strict-peer-deps=true
